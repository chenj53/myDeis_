<%- include('bootheader') -%>
<%- include('menubar') -%>
    <div class="jumbotron">
      <div class="media-body">
          <h4 class="media-heading"> <font size="6"> <%= post.post %>   </a></font> - <%= post.course %>  </h4>
          <p class="text-left"> <strong> <%= post.condition %> </p>
          <p class="text-left">  $ <%= post.price  %>   </p>
          <p class="text-left"> <%= post.contact  %>  : <%= post.contactInfo %>   </p>
          <p class="text-right"> By <%= post.userName %> </p>
          <p class="text-right">  <%= status %> </p>
          <p>  Description: <%= post.description %> </p>
          </strong>
    </div>
    </div>

           <p class="text-left"> <strong> <%= post.condition %> </p>
           <p class="text-left">  $ <%= post.price  %>   </p>
           <p class="text-left"> <%= post.contact  %>  : <%= post.contactInfo %>   </p>

   <% if (post.userId.equals(user._id)) { %>

   <% } %>

   <pre>

   </pre>



   <% console.log("in forumPost.ejs with comments.length ="+comments.length) %>
   <% for(i = 0; i<comments.length; i++){  %>
     <style>
           .well {
               height: 50px;
               width: 100%;
               color: white;
               background-color: black;
           }
      </style>
  <body>
      <div class="well">
        <li> <a href="/showComment/<%= comments[i]._id %>">
              <%= comments[i].userName %> : </a>
              <%= comments[i].comment %> <br>
              <% if (post.userId.equals(user._id)) { %>
        <input type="checkbox" name="delete" value="<%= post._id %>">
    <% } %>

              <%= comments[i].createdAt %></\/i> <br>
         </li>
      </div>
      </body>



   <% } %>
   <br>


       <li>
          <strong>   <%= comments[i].userName %></a>
             <i>
            <%= comments[i].createdAt %></\/i> <br>
            <% if (comments[i].userId.equals(user._id)) { %>

            <% } %>
            <br> </strong>
            <pre><%= comments[i].comment %></pre>
        </li>
   <% } %>
   </ol>


<%- include('bootfooter') -%>
